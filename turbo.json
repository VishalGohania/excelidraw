{
  "$schema": "https://turbo.build/schema.json",
  "ui": "tui",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "inputs": ["$TURBO_DEFAULT$", ".env*"],
      "outputs": [".next/**", "!.next/cache/**", "dist/**"]
    },
    "@repo/db#build": {
      "dependsOn": ["@repo/db#db:generate"],
      "outputs": ["dist/**"]
    },
    "@repo/backend-common#build": {
      "outputs": ["dist/**"]
    },
    "@repo/common#build": {
      "outputs": ["dist/**"]
    },
    "http-backend#build": {
      "dependsOn": ["@repo/db#build", "@repo/backend-common#build", "@repo/common#build"],
      "outputs": ["dist/**"]
    },
    "db:generate": {
      "cache": false,
      "outputs": ["node_modules/.prisma/**"]
    },
    "lint": {
      "dependsOn": ["^lint"]
    },
    "check-types": {
      "dependsOn": ["^check-types"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    }
  }
}
